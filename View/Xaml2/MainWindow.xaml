<Window x:Class="Fork.View.Xaml2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:Fork.View.Xaml2.Controls"
        xmlns:shapes="clr-namespace:Fork.View.Xaml2.Shapes"
        xmlns:converter="clr-namespace:Fork.View.Xaml.Converter"
        xmlns:dd="urn:gong-wpf-dragdrop"
        xmlns:model="clr-namespace:Fork.Logic.Model"
        mc:Ignorable="d" MinWidth="1080" MinHeight="400"
        Title="Fork" Height="1080" Width="1920"
        WindowState="Maximized" WindowStartupLocation="Manual" Left="0" Top="0"
        Style="{DynamicResource DefaultWindowV2}">
    <Window.Resources>
        <converter:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter" />
        <converter:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </Window.Resources>
    <Grid>
        <DockPanel Background="{DynamicResource backgroundDefault}">
            <Grid Panel.ZIndex="100">
                <Border Margin="0 0 5 0" Background="{DynamicResource backgroundDark}" />
                <Grid Panel.ZIndex="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="140" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="5*" />
                        <RowDefinition Height="Auto" MinHeight="120" />
                    </Grid.RowDefinitions>
                    <Grid Margin="0 0 5 0" Background="{DynamicResource backgroundDark}">
                        <Image Margin="20" RenderOptions.BitmapScalingMode="HighQuality">
                            <Image.Style>
                                <Style TargetType="Image">
                                    <Setter Property="Source" Value="{Binding Boi}" />
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Source" Value="{Binding BoiHover}" />
                                        </Trigger>
                                        <DataTrigger Binding="{Binding HasServers}" Value="False">
                                            <Setter Property="Visibility" Value="Hidden" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Image.Style>
                        </Image>
                    </Grid>
                    <StackPanel Margin="0 8 5 8" Grid.Row="1" Orientation="Vertical" HorizontalAlignment="Center">
                        <Button x:Name="AppSettingsButton" Content="Settings" Width="185" Margin="0 4"
                                Style="{DynamicResource RoundedTextButton}"
                                Click="OpenAppSettings_Click" />
                    </StackPanel>
                    <Grid Grid.Row="2" VerticalAlignment="Stretch" MinWidth="243">
                        <Border Background="{DynamicResource backgroundDark}" Margin="0 0 5 0" />
                        <Grid Panel.ZIndex="1">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition />
                            </Grid.RowDefinitions>
                            <TextBlock Text="Server List" Margin="15 10 10 10"
                                       Foreground="{DynamicResource textUnselected}" FontSize="20"
                                       FontFamily="{StaticResource Montserrat-SemiBold}"
                                       Visibility="{Binding HasServers, Converter={StaticResource BoolToVisibilityConverter}}" />

                            <ListView Margin="0" Grid.Row="1" Background="{DynamicResource backgroundDark}"
                                      Style="{StaticResource ServerListViewV2}"
                                      ItemsSource="{Binding Entities}" x:Name="ServerList"
                                      dd:DragDrop.IsDragSource="True" dd:DragDrop.IsDropTarget="True"
                                      SelectedItem="{Binding SelectedEntity}"
                                      SelectionChanged="ServerList_SelectionChanged">
                                <!-- This shouldn't be here, but Event handling is shit -->
                                <ListView.ItemContainerStyle>
                                    <Style TargetType="{x:Type ListViewItem}">
                                        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                                        <Setter Property="Background" Value="Transparent" />
                                        <Setter Property="Foreground" Value="{StaticResource foregroundDefault}" />
                                        <Setter Property="Margin" Value="0 2 0 1" />
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="{x:Type ListViewItem}">
                                                    <Border BorderBrush="Transparent" BorderThickness="0"
                                                            Background="{TemplateBinding Background}">
                                                        <StackPanel MouseDown="EntityMouseDown" MouseUp="EntityMouseUp">
                                                            <Grid Width="250" Height="35">
                                                                <Border x:Name="background" Background="Transparent"
                                                                        Width="251" CornerRadius="0 8 8 0"
                                                                        Margin="-1 0 0 0" />
                                                                <DockPanel Panel.ZIndex="1" LastChildFill="True">
                                                                    <Ellipse x:Name="dot" Margin="10 0 0 0" Width="10"
                                                                             Height="10" Fill="{Binding IconColor}" />
                                                                    <Border Height="26" Width="26"
                                                                            Background="Transparent" Margin="3 0 6 0">
                                                                        <Image x:Name="image" Source="{Binding Icon}"
                                                                               Width="16" Height="16"
                                                                               RenderOptions.BitmapScalingMode="Linear" />
                                                                    </Border>
                                                                    <TextBlock x:Name="title" Text="{Binding Name}"
                                                                               VerticalAlignment="Center" FontSize="13"
                                                                               FontFamily="{DynamicResource Montserrat-SemiBold}"
                                                                               Foreground="{DynamicResource textUnselected}"
                                                                               Margin="0 0 10 0" />
                                                                </DockPanel>
                                                                <Grid.ContextMenu>
                                                                    <ContextMenu>
                                                                        <MenuItem Header="Rename"
                                                                            Style="{DynamicResource ContextMenuItem}"
                                                                            Click="RenameOpen_Click" />
                                                                        <MenuItem Header="Clone"
                                                                            Style="{DynamicResource ContextMenuItem}"
                                                                            Click="CloneOpen_Click" />
                                                                        <MenuItem Header="Delete"
                                                                            Style="{DynamicResource RedContextMenuItem}"
                                                                            Click="DeleteOpen_Click" />
                                                                    </ContextMenu>
                                                                </Grid.ContextMenu>
                                                            </Grid>
                                                        </StackPanel>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsSelected" Value="True">
                                                            <Setter TargetName="background" Property="Background"
                                                                    Value="{DynamicResource tabSelected}" />
                                                            <Setter TargetName="title" Property="Foreground"
                                                                    Value="{DynamicResource textSelected}" />
                                                            <Setter TargetName="image" Property="Source"
                                                                    Value="{Binding IconW}" />
                                                            <Setter TargetName="dot" Property="Fill"
                                                                    Value="{Binding IconColorHovered}" />
                                                        </Trigger>
                                                        <Trigger Property="IsSelected" Value="False">
                                                            <Setter TargetName="dot" Property="Fill"
                                                                    Value="{Binding IconColor}" />
                                                            <Setter Property="Cursor" Value="Hand" />
                                                        </Trigger>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter TargetName="title" Property="Foreground"
                                                                    Value="{DynamicResource textSelected}" />
                                                            <Setter TargetName="image" Property="Source"
                                                                    Value="{Binding IconW}" />
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </ListView.ItemContainerStyle>
                            </ListView>
                        </Grid>
                    </Grid>
                    <DockPanel Grid.Row="3" LastChildFill="False" Margin="0 0 5 0"
                               Background="{DynamicResource backgroundDark}">
                        <StackPanel DockPanel.Dock="Bottom" Background="{DynamicResource backgroundDarker}">
                            <Border CornerRadius="0 10 10 0" Height="55" Margin="0 3 -5 3"
                                    Background="{DynamicResource buttonBgrBlue}"
                                    Visibility="{Binding NewerVersionExists, Converter={StaticResource BoolToVisibilityConverter}}">
                                <Button IsEnabled="False">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border CornerRadius="0 10 10 0" Padding="20 0 16 0"
                                                                Background="Transparent" Cursor="Hand"
                                                                PreviewMouseLeftButtonDown="NewVersion_PreviewMouseLeftButtonDown">
                                                            <Grid>
                                                                <DockPanel>
                                                                    <Image x:Name="icon"
                                                                           Source="pack://application:,,,/View/Resources/images/Icons/Download.png"
                                                                           DockPanel.Dock="Right" Height="20"
                                                                           VerticalAlignment="Center"
                                                                           RenderOptions.BitmapScalingMode="HighQuality" />
                                                                    <Grid VerticalAlignment="Center">
                                                                        <Grid.RowDefinitions>
                                                                            <RowDefinition />
                                                                            <RowDefinition />
                                                                        </Grid.RowDefinitions>
                                                                        <StackPanel Orientation="Horizontal">
                                                                            <TextBlock x:Name="ver1" Text="Version "
                                                                                FontFamily="{DynamicResource Montserrat-Bold}"
                                                                                FontSize="17"
                                                                                Foreground="{DynamicResource backgroundDarker}" />
                                                                            <TextBlock x:Name="ver2"
                                                                                Text="{Binding LatestForkVersion.Major}"
                                                                                FontFamily="{DynamicResource Montserrat-Bold}"
                                                                                FontSize="17"
                                                                                Foreground="{DynamicResource backgroundDarker}" />
                                                                            <TextBlock x:Name="ver3" Text="."
                                                                                FontFamily="{DynamicResource Montserrat-Bold}"
                                                                                FontSize="17"
                                                                                Foreground="{DynamicResource backgroundDarker}" />
                                                                            <TextBlock x:Name="ver4"
                                                                                Text="{Binding LatestForkVersion.Minor}"
                                                                                FontFamily="{DynamicResource Montserrat-Bold}"
                                                                                FontSize="17"
                                                                                Foreground="{DynamicResource backgroundDarker}" />
                                                                            <TextBlock x:Name="ver3b" Text="."
                                                                                FontFamily="{DynamicResource Montserrat-Bold}"
                                                                                FontSize="17"
                                                                                Foreground="{DynamicResource backgroundDarker}" />
                                                                            <TextBlock x:Name="ver5"
                                                                                Text="{Binding LatestForkVersion.Patch}"
                                                                                FontFamily="{DynamicResource Montserrat-Bold}"
                                                                                FontSize="17"
                                                                                Foreground="{DynamicResource backgroundDarker}" />
                                                                            <TextBlock x:Name="ver9"
                                                                                Text=" is out"
                                                                                FontFamily="{DynamicResource Montserrat-Bold}"
                                                                                FontSize="17"
                                                                                Foreground="{DynamicResource backgroundDarker}" />
                                                                        </StackPanel>
                                                                        <StackPanel Grid.Row="1">
                                                                            <TextBlock x:Name="ver10"
                                                                                Text="Restart Launcher to Update"
                                                                                Foreground="{DynamicResource backgroundDarker}" />
                                                                        </StackPanel>
                                                                    </Grid>
                                                                </DockPanel>
                                                            </Grid>
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </Border>
                            <Label HorizontalAlignment="Center" FontFamily="{DynamicResource Montserrat-SemiBold}"
                                   FontSize="11" VerticalAlignment="Top" Foreground="White" Padding="5 2">
                                <StackPanel Orientation="Horizontal" MaxWidth="200">
                                    <TextBlock Text="v" />
                                    <TextBlock Text="{Binding CurrentForkVersion.Major}" />
                                    <TextBlock Text="." />
                                    <TextBlock Text="{Binding CurrentForkVersion.Minor}" />
                                    <TextBlock Text="." />
                                    <TextBlock Text="{Binding CurrentForkVersion.Patch}" />
                                </StackPanel>
                            </Label>
                        </StackPanel>
                        <Grid DockPanel.Dock="Bottom" Margin="10">
                            <StackPanel Orientation="Vertical" HorizontalAlignment="Center" Margin="0 0 0 10">
                                <Button x:Name="CreateButton" Content="New Server" Width="185" Margin="0 4"
                                        Style="{DynamicResource RoundedTextButtonGreen}"
                                        Click="CreateServer_Click" />
                                <Button x:Name="ImportButton" Content="Import Server" Width="185" Margin="0 4"
                                        Style="{DynamicResource RoundedTextButton}"
                                        Click="ImportServer_Click" />
                                <Button x:Name="DeleteButton" Content="Delete" Width="185" Margin="0 4"
                                        Style="{DynamicResource RoundedTextButtonRed}"
                                        Click="DeleteOpen_Click" />
                            </StackPanel>
                        </Grid>
                    </DockPanel>
                </Grid>
            </Grid>
            <Grid>
                <!-- Default screen -->
                <Grid Visibility="{Binding HasServers, Converter={StaticResource InverseBoolToVisibilityConverter}}">
                    <controls:EmptyStateView
                        CreateServerRequested="CreateServer_Click"
                        ImportServerRequested="ImportServer_Click" />
                </Grid>

                <!-- Subscreens -->
                <Border Visibility="{Binding HasServers, Converter={StaticResource BoolToVisibilityConverter}}">
                    <Frame x:Name="ServerPage" DataContext="{Binding SelectedEntity, Mode=TwoWay}"
                           Content="{Binding EntityPage}" NavigationUIVisibility="Hidden"
                           Background="{DynamicResource backgroundDefault}" />
                </Border>
                <Frame x:Name="CreatePage" Grid.Column="1" Content="{Binding CreatePage}"
                       NavigationUIVisibility="Hidden" Visibility="Hidden"
                       Background="{DynamicResource backgroundDefault}" />
                <Frame x:Name="ImportPage" Grid.Column="1" Content="{Binding ImportPage}"
                       NavigationUIVisibility="Hidden" Visibility="Hidden"
                       Background="{DynamicResource backgroundDefault}" />
                <Frame x:Name="AppSettingsPage" Grid.Column="1"
                       Content="{Binding AppSettingsViewModel.AppSettingsPage}"
                       NavigationUIVisibility="Hidden" Visibility="Hidden"
                       Background="{DynamicResource backgroundDefault}" />
            </Grid>
        </DockPanel>

        <!-- Confirmation overlays -->
        <controls:ConfirmationOverlay x:Name="DeleteServerOverlay" Panel.ZIndex="1"
            OverlayTitle="Delete Server"
            WarningText="WARNING: This action will permanently remove your beloved server"
            ConfirmButtonText="Delete"
            IsDestructive="True"
            ConfirmClicked="DeleteServer_Confirmed"
            CancelClicked="Overlay_Cancelled" />

        <controls:ConfirmationOverlay x:Name="DeleteNetworkOverlay" Panel.ZIndex="1"
            OverlayTitle="Delete Network"
            WarningText="WARNING: This action will permanently remove your beloved network"
            ConfirmButtonText="Delete"
            IsDestructive="True"
            ConfirmClicked="DeleteNetwork_Confirmed"
            CancelClicked="Overlay_Cancelled" />

        <controls:ConfirmationOverlay x:Name="RenameServerOverlay" Panel.ZIndex="1"
            OverlayTitle="Rename Server"
            WarningText="WARNING: Renaming will stop your Server"
            ConfirmButtonText="Rename"
            ShowInput="True"
            ConfirmClicked="RenameServer_Confirmed"
            CancelClicked="Overlay_Cancelled" />

        <controls:ConfirmationOverlay x:Name="RenameNetworkOverlay" Panel.ZIndex="1"
            OverlayTitle="Rename Network"
            WarningText="WARNING: Renaming will stop your Network"
            ConfirmButtonText="Rename"
            ShowInput="True"
            ConfirmClicked="RenameNetwork_Confirmed"
            CancelClicked="Overlay_Cancelled" />

        <controls:ConfirmationOverlay x:Name="CloneServerOverlay" Panel.ZIndex="1"
            OverlayTitle="Clone Server"
            WarningText="WARNING: Cloning will stop your Server"
            ConfirmButtonText="Clone"
            ConfirmClicked="CloneServer_Confirmed"
            CancelClicked="Overlay_Cancelled" />

        <controls:ConfirmationOverlay x:Name="CloneNetworkOverlay" Panel.ZIndex="1"
            OverlayTitle="Clone Network"
            WarningText="WARNING: Cloning will stop your Network"
            ConfirmButtonText="Clone"
            ConfirmClicked="CloneNetwork_Confirmed"
            CancelClicked="Overlay_Cancelled" />

        <!-- Java warning overlay -->
        <controls:JavaWarningOverlay Panel.ZIndex="1"
            Visibility="{Binding ShowJavaWarning, Converter={StaticResource BoolToVisibilityConverter}}"
            DataContext="{Binding}" />
    </Grid>
</Window>
